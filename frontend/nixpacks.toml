[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "NITRO_HOST=0.0.0.0 NITRO_PORT=$PORT node .output/server/index.mjs"
