{
  "name": "tucancha-sena",
  "version": "1.0.0",
  "private": true,
  "description": "Tu Cancha — frontend Nuxt + backend NestJS + PostgreSQL",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "setup": "npm run install:all && npm run db:migrate && npm run db:seed",
    "dev": "concurrently -n backend,frontend -c blue,green \"npm:dev:backend\" \"npm:dev:frontend\"",
    "dev:backend": "npm --prefix backend run start:dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "db:migrate": "npm --prefix backend run prisma:migrate -- --name init",
    "db:seed": "npm --prefix backend run db:seed",
    "db:studio": "npm --prefix backend run prisma:studio"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
